window.PLANT_V3_CONFIG = {
  apiBaseUrl: "/api",
  deviceId: "",
  staleTimeoutSec: 15,
  refreshMs: 3000
};
// Cloudflare Pages + Workers runtime. D1 database: plantCareMainDB.
// Never put customer passwords, device secrets, factory keys or other long-lived secrets here.
